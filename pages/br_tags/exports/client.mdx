---
title: Client
---

## has
Returns a `boolean` indicating if the specified player has that tag
```lua showLineNumbers
if exports["br_tags"]:has(tag) then
    print("I have the tag")
end
```
- tag: `string`
    - Tag's name