import { Button } from "@mui/material"
import GitHubIcon from "@mui/icons-material/GitHub"
import ReleaseIcon from "@mui/icons-material/Sell"
import { Steps, Callout, FileTree } from "nextra/components"

# br_player

***

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/Brodino96/br_player") }} > 
    Repo
</Button>

<Button variant="outlined" color="" endIcon={<ReleaseIcon />} onClick={() => { open("https://github.com/Brodino96/br_player/releases") }} > 
    Release
</Button>

**It's an audio player... I don't know what are you expecting from me...**

***

<FileTree>
    <FileTree.Folder name="html">
        <FileTree.Folder name="sounds">
            <FileTree.File name="demo.ogg" />
        </FileTree.Folder>
        <FileTree.File name="index.html" />
    </FileTree.Folder>
    <FileTree.File name="client.lua" />
    <FileTree.File name="server.lua" />
    <FileTree.File name="fxmanifest.lua                        " />
</FileTree>

# Installation

<Steps>
### Download a [release](https://github.com/Brodino96/br_player/releases/latest) or clone the repository
```shell filename="console" showLineNumbers
git clone https://github.com/Brodino96/br_player
```

### Put your audio files inside the folder `html/sounds/`
<Callout type="error" emoji="️🚫">
    You can only use file formats supported by browsers
</Callout>

</Steps>