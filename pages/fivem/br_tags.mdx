import { Button } from "@mui/material"
import GitHubIcon from "@mui/icons-material/GitHub"
import ReleaseIcon from '@mui/icons-material/Sell'
import { Steps, Callout, FileTree } from "nextra/components"

# br_tags

***

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/Brodino96/br_tags") }} > 
  Repo
</Button>

<Button variant="outlined" color="" endIcon={<ReleaseIcon />} onClick={() => { open("https://github.com/Brodino96/br_tags/releases") }} > 
  Release
</Button>

**A system to assign tags to players in your server**

<Callout type="info">
  In the future i want to provide an external website to edit your in game tags
</Callout>

***

<FileTree>
  <FileTree.Folder name="client">
    <FileTree.File name="main.lua" />
    <FileTree.File name="nui.lua" />
  </FileTree.Folder>
  <FileTree.Folder name="server">
    <FileTree.File name="database.lua" />
    <FileTree.File name="main.lua" />
    <FileTree.File name="menu.lua" />
  </FileTree.Folder>
    <FileTree.Folder name="web">
    <FileTree.File name="index.html" />
    <FileTree.File name="script.js" />
    <FileTree.File name="style.css" />
  </FileTree.Folder>
  <FileTree.File name="config.lua" />
  <FileTree.File name="database.sql" />
  <FileTree.File name="fxmanifest.lua                        " />
</FileTree>

## Installation

<Steps>
### Download a [release](https://github.com/Brodino96/br_tags/releases/latest) or clone the repository
```shell filename="console" showLineNumbers
git clone https://github.com/Brodino96/br_tags
```

### Prepare database using the file `database.sql`
<Callout type="error" emoji="️🚫">
  The script won't work if you miss this step
</Callout>

### Update the configs using the file `config.lua`

</Steps>

## Dependencies
<br/><br/>
<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/esx-framework/esx_core") }} > 
  esx
</Button>

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/overextended/ox_lib") }} > 
  ox_lib
</Button>

You must have these scripts or you won't be able to use this system