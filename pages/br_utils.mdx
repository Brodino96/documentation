import { Button } from "@mui/material"
import GitHubIcon from "@mui/icons-material/GitHub"
import ReleaseIcon from "@mui/icons-material/Sell"
import { Steps, Callout, FileTree } from "nextra/components"

# br_utils

***

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/Brodino96/br_utils") }} > 
  Repo
</Button>

<Button variant="outlined" color="" endIcon={<ReleaseIcon />} onClick={() => { open("https://github.com/Brodino96/br_utils/releases") }} > 
  Release
</Button>

**A utility package with usefull events**

***

<FileTree>
  <FileTree.Folder name="client">
    <FileTree.Folder name="modules">
      <FileTree.File name="airTurbulence.lua" />
      <FileTree.File name="autoHelmet.lua" />
      <FileTree.File name="idleCamera.lua" />
      <FileTree.File name="onPedChange.lua" />
      <FileTree.File name="onPlayerDeath.lua" />
      <FileTree.File name="onVehicleChange.lua" />
      <FileTree.File name="playerAttributes.lua" />
      <FileTree.File name="playerLoaded.lua" />
      <FileTree.File name="weaponDamage.lua" />
    </FileTree.Folder>
    <FileTree.File name="main.lua" />
  </FileTree.Folder>

  <FileTree.Folder name="server">
    <FileTree.Folder name="modules">
      <FileTree.File name="isStaff.lua" />
    </FileTree.Folder>
    <FileTree.File name="main.lua" />
    <FileTree.File name="versionCheck.lua" />
  </FileTree.Folder>

  <FileTree.Folder name="shared">
    <FileTree.File name="debug.lua" />
    <FileTree.File name="framework.lua" />
  </FileTree.Folder>
  <FileTree.File name="config.lua" />
  <FileTree.File name="fxmanifest.lua                        " />
</FileTree>


## Installation

<Steps>
### Download a [release](https://github.com/Brodino96/br_utils/releases/latest) or clone the repository
```shell filename="console" showLineNumbers
git clone https://github.com/Brodino96/br_utils.git
```

### Configure the script using the file `config.lua`

</Steps>

## Supported frameworks
<br></br>
<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/esx-framework/esx_core") }} > 
  esx
</Button>

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/overextended/ox_core") }} > 
  ox_core
</Button>

<Button variant="outlined" color="" endIcon={<GitHubIcon />} onClick={() => { open("https://github.com/qbcore-framework/qb-core") }} > 
  qbcore
</Button>

You must use one of these framework or some features won't work